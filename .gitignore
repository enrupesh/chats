node_modules
dist
build
.cache
.turbo
.vite
*.log
.DS_Store

# Android build artefacts (generated by Gradle — never commit)
apps/client/android/.gradle/
apps/client/android/build/
apps/client/android/app/build/
apps/client/android/app/release/
apps/client/android/.idea/
apps/client/android/local.properties
# Keystore — NEVER commit signing keys
*.jks
*.keystore
# Play Store icon (regenerated by script)
apps/client/android-playstore-icon.png

# Env files
.env
.env.local
.env.*.local

# Editor
.vscode
.idea

# Replit
.replit.lock

# Block accidental commits of credentials
attached_assets/*service*.json
attached_assets/*adminsdk*.json
attached_assets/*firebase*.json
attached_assets/*credentials*
attached_assets/Pasted*service*
attached_assets/Pasted*private_key*
